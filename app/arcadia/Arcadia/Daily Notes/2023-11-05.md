---
tags:
  - daily
mood: <empty>
---

## 🧨 Tasks
```tasks
not done
(scheduled before 2023-11-05) OR (due before 2023-11-05)
```

## 📈 Stats 
*Things to celebrate on!*

### 🏆 Tasks completed

|Month|Yesterday|Today|
|------|----------|------|
|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2023-11-05').setDate(1)).toISOString().slice(0, 7))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2023-11-05') - 86400000).toISOString().slice(0, 10))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ 2023-11-05')).length`|


## 🥳 Done Today!
```tasks
done 2023-11-05
```


## 🪵 Log
*What's on my mind today?*


Make a function that recursively goes through an object and convert dotGrid into URLthe URL.

^ call this function after the data is applied to the template? using regular string replace? i.e.:
- Replace all occurrence of `"backgroundImage": "dotGrid"` with `"backgroundImage": "<dot-grid-url>"` ✅ I think this has the simplest execution


or should we do 2 pass of template application?
1. the first apply applies `"backgroundImage": "{{dotGrid}}"`
2. Which will then be applied again with the data.


Or...
1. Convert required data into string, ask the AI to return `{{dotgrid}}` instead of `dotGrid`?
	1. How do you make sure that the AI doesn't return `dotGrid`?


Final thoughts:
I think we should go through the `requiredData` returned by the AI and pre-process it to be inserted into the template. This includes:
1. assigning default values
2. replacing background images with the actual URLs.

