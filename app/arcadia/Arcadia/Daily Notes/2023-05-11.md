---
tags: daily
mood: <empty>
---

## 🧨 Tasks
```tasks
not done
(scheduled before 2023-05-11) OR (due before 2023-05-11)
```

## 📈 Stats 
*Things to celebrate on!*

### 🏆 Tasks completed
|Month|Yesterday|Today|
|------|----------|------|
|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2023-05-11').setDate(1)).toISOString().slice(0, 7))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2023-05-11') - 86400000).toISOString().slice(0, 10))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ 2023-05-11')).length`|


## 🥳 Done Today!
```tasks
done 2023-05-11
```


## 🪵 Log
*What's on my mind today?*

NavbarContainerOverlay visibility logic is quite confusing:
1. It needs to know whether it should show when:
	1. the user change the menu item type of the dropdown menu that it is currently showing
	2. 
