---
tags: daily
mood: <empty>
---

## 🧨 Tasks
```tasks
not done
(scheduled before 2023-05-18) OR (due before 2023-05-18)
```

## 📈 Stats 
*Things to celebrate on!*

### 🏆 Tasks completed
|Month|Yesterday|Today|
|------|----------|------|
|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2023-05-18').setDate(1)).toISOString().slice(0, 7))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2023-05-18') - 86400000).toISOString().slice(0, 10))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ 2023-05-18')).length`|


## 🥳 Done Today!
```tasks
done 2023-05-18
```


## 🪵 Log
*What's on my mind today?*

- Create a function:
	- Given a list of form ids, duplicate those ids, return the duplicated results
		- Should it immediately handle duplicate? or should it get the forms first, then re-create them?
		- Should we do it sequentially or all at once and return all the duplicated collections?

