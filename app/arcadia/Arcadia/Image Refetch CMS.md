`apps/renderer/features/collection/itemPageWrapper.tsx 
See `useItemPageFetcher`
The frontend logic to refetch is here.

`packages/data/src/utils/notionBlockSync.ts`
See `getAdditionalProps`
for notion Sync, images are reuploaded to server, while regular CMS directly refetch from notion.

