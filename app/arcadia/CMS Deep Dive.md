[[CMS data flow]]
[[CMS V2]]


Major Problems:
- Data vs View. Storing collection_data directly inside the SlateJSON, template schema, etc.
- No tests
- **Naming**, It's very difficult to understand what `collectionViewSchema` vs `itemPageContentSchema` and many more actually are.

[[CMS Airtable]]