---
tags: daily
mood: <empty>
---

## 🧨 Tasks
```tasks
not done
(scheduled before 2024-03-19) OR (due before 2024-03-19)
```

## 📈 Stats 
*Things to celebrate on!*

### 🏆 Tasks completed

|Month|Yesterday|Today|
|------|----------|------|
|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2024-03-19').setDate(1)).toISOString().slice(0, 7))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2024-03-19') - 86400000).toISOString().slice(0, 10))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ 2024-03-19')).length`|


## 🥳 Done Today!
```tasks
done 2024-03-19
```


## 🪵 Log
*What's on my mind today?*

From WhatsApp chat:
- process the uploaded text file (s3 storage, etc)
- clean up the original text message
	- remove gif, video, audio omitted lines
	- Reduce token: simplify names, remove empty lines combine them into single text escaped newline characters
- Extract knowledge/data:
	- Entities
	- Personality traits
	- 

