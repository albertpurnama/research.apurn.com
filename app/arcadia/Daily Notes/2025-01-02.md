---
tags: daily
mood: <empty>
---

## 🧨 Tasks
```tasks
not done
(scheduled before 2025-01-02) OR (due before 2025-01-02)
```

## 📈 Stats 
*Things to celebrate on!*

### 🏆 Tasks completed

|Month|Yesterday|Today|
|------|----------|------|
|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2025-01-02').setDate(1)).toISOString().slice(0, 7))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2025-01-02') - 86400000).toISOString().slice(0, 10))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ 2025-01-02')).length`|


## 🥳 Done Today!
```tasks
done 2025-01-02
```


## 🪵 Log
*What's on my mind today?*


Create staging page locally:
1. heroku run bin/rails c -a beehiiv-swarm-staging
2. Grab the page content SiteTemplate.findx(pageid).content
3. content = (2)
4. Grab the page id on local. set the content to (2) Page.find(page-id-local).draft_page_version.update(content: (2))
5. 

