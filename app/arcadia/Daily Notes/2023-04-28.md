---
tags: daily
mood: 5
---

## 🧨 Tasks
```tasks
not done
(scheduled before 2023-04-28) OR (due before 2023-04-28)
```

## 📈 Stats 
*Things to celebrate on!*

### 🏆 Tasks completed
|Month|Yesterday|Today|
|------|----------|------|
|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2023-04-28').setDate(1)).toISOString().slice(0, 7))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ ' + new Date(new Date('2023-04-28') - 86400000).toISOString().slice(0, 10))).length`|`$= dv.pages().file.tasks.where(t => t.completed).where(t => t.text.includes('✅ 2023-04-28')).length`|


## 🥳 Done Today!
```tasks
done 2023-04-28
```


## 🪵 Log
*What's on my mind today?*

1. Create pinecone account, with pinecone index dimension 1536
	1. We should use user id as namespace, and document title as metadata.
2. 

[[DocsGPT]]
You can delete indexes by namespace, so we need to store namespace that ties to the user id.
namespace = `${user-id}`
we put the document title in the metadata, so we can query the indexes based on the `filter` of `PineconeStore.fromExistingIndex`

The database can have

1. Upload multiple file frontend
2. Handle loading the multiple file
3. Create vector index, with namespace for user, metadata for the files save it to pinecone
4. 

